#ifndef  JIUGESTL_HEADFILE_ALLOCATOR
#define  JIUGESTL_HEADFILE_ALLOCATOR
#include "util.h"
namespace JiugeStl
{
template <typename T>
class allocator
{
public:
	allocator();
	~allocator();
	
private:

};

template <typename T>
allocator::allocator()
{
}

template <typename T>
allocator::~allocator()
{
}
}
#endif